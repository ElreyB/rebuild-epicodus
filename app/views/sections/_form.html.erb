<% render("layouts/errors", :object => @section) %>

<%= form_for [@chapter, @section] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :number %>
  <%= f.number_field :number %>

  <%= f.submit %>
<% end %>
